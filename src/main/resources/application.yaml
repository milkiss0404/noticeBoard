spring:
  profiles:
    default: local


  datasource:
    url: jdbc:h2:mem:~/notice_board
    driver-class-name: org.h2.Driver
    username: sa
    password:
#logging:
#  level:
#    root: debug


  jpa:
    hibernate:
      ddl-auto: none
  h2:
    console:
      enabled: true
      path: /h2-console

---

spring:
  config:
    activate:
      on-profile: local
  h2:
    console:
      enabled: true
      path: /h2-console

  jpa:
    hibernate:
      ddl-auto: create
    show-sql:  true
    properties:
      hibernate:
        format_sql: true
    defer-datasource-initialization: true
jwt:
  secret: dsadsadsamnzxvzclklewqioewqo!!#@#$@$@$@$@)$(@%(@(%(


---

spring:
  config:
    activate:
      on-profile: test

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true


  sql:
    init:
      mode: never
